# Cloudflare Pages (Git connected)
#
# Build command:     npm run build:public
# Build output:      dist
# Deploy command:    LEAVE EMPTY (do not use wrangler)
# Node:              22 (uses .node-version)
#
# Do NOT add wrangler.toml — it breaks static deploys.
