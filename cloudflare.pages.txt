# Cloudflare (Git connected — Workers Builds UI)
#
# If Deploy Command is REQUIRED (cannot be blank), use Workers static assets:
#
# Build command:     npm run build:public
# Deploy command:    npx wrangler deploy
# Root directory:    (blank)
#
# wrangler.jsonc points at ./dist — no separate "output directory" field needed.
#
# Site URL will be:  https://questlife.<your-subdomain>.workers.dev
#
# Alternative if you created a classic Pages project instead:
#   Deploy command:  npx wrangler pages deploy dist --project-name=questlife
